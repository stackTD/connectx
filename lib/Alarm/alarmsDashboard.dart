import 'package:flutter/material.dart';

class AlarmsDashboard extends StatelessWidget {
  const AlarmsDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Work in progress',
        style: TextStyle(
          fontSize: 24,
          fontWeight: FontWeight.bold,
          color: Colors.black87,
        ),
      ),
    );
  }
}
